# Cloudflare Pages Configuration for static site
[build]
  command = "echo Ready"
  cwd = "."

[site]
  bucket = "."
# GitHub Actions deploy uses this — build first then deploy
[build]
  command = "npm install && npm run build"
  cwd = "."
